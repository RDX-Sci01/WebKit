name: Sync slopkit2

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:

      # ============================================================
      # 1. Checkout YOUR repository
      # ============================================================
      - name: Checkout repository
        uses: actions/checkout@v4.2.2
        with:
          fetch-depth: 0

      # ============================================================
      # 2. Download upstream repository
      # ============================================================
      - name: Download upstream slopkit
        run: |
          set -euo pipefail

          echo "Downloading upstream repository..."

          git clone \
            --depth 1 \
            https://github.com/Not-Yitan/ChileServer-Slopkit.git \
            upstream

          echo "Download completed."

      # ============================================================
      # 3. Verify upstream exists and has index.html
      # ============================================================
      - name: Validate upstream folder
        run: |
          set -euo pipefail

          if [ ! -d "upstream" ]; then
            echo "::error::Upstream clone failed — directory missing."
            exit 1
          fi

          if [ ! -f "upstream/index.html" ]; then
            echo "::error::upstream/index.html is missing. Aborting."
            exit 1
          fi

          echo "upstream/index.html exists."

      # ============================================================
      # 4. Make sure upstream is NOT empty
      # ============================================================
      - name: Validate files
        run: |
          set -euo pipefail

          FILE_COUNT=$(find upstream -type f -not -path "upstream/.git/*" | wc -l)
          echo "Upstream contains $FILE_COUNT files."

          if [ "$FILE_COUNT" -eq 0 ]; then
            echo "::error::Upstream is EMPTY. Aborting to protect your copy."
            exit 1
          fi

          echo "Folder contains files."

      # ============================================================
      # 5. Prepare new copy (strip .git and symlinks)
      # ============================================================
      - name: Prepare new slopkit2 copy
        run: |
          set -euo pipefail

          rm -rf new-slopkit2

          rsync -a --exclude='.git' upstream/ new-slopkit2/

          # Remove any symlinks defensively
          find new-slopkit2 -type l -exec rm -f {} +

          echo "Temporary copy prepared."

      # ============================================================
      # 6. Preserve mirror payloads across sync
      #
      # The upstream has its own payloads/ folder (pldmgr_v0.5.1.elf
      # etc). We keep those BUT also layer our mirror payloads on top
      # so nothing from sync_payloads.yml gets lost.
      #
      # Priority: mirror payloads win over upstream payloads on
      # filename conflict, since they are versioned and checksummed.
      # ============================================================
      - name: Preserve mirror payloads
        run: |
          set -euo pipefail

          if [ -d "slopkit2/payloads" ]; then
            echo "Merging existing mirror payloads into new copy..."

            # new-slopkit2/payloads already has upstream payloads.
            # Copy mirror payloads on top — rsync skips identical files.
            rsync -a slopkit2/payloads/ new-slopkit2/payloads/

            echo "Mirror payloads preserved."
          else
            echo "No existing mirror payloads to preserve."
          fi

      # ============================================================
      # 7. Replace slopkit2
      # ============================================================
      - name: Update slopkit2
        run: |
          set -euo pipefail

          rm -rf slopkit2
          mv new-slopkit2 slopkit2

          echo "slopkit2 updated."

      # ============================================================
      # 8. Stage files BEFORE checking for changes
      # ============================================================
      - name: Stage changes
        run: |
          set -euo pipefail

          git add slopkit2

          echo "Git status:"
          git status --short

      # ============================================================
      # 9. Determine whether anything changed
      # ============================================================
      - name: Check for changes
        id: changes
        run: |
          set -euo pipefail

          if git diff --cached --quiet; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "No changes detected."
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
            echo "Changes detected."
          fi

      # ============================================================
      # 10. Commit changes
      # ============================================================
      - name: Commit changes
        if: steps.changes.outputs.changed == 'true'
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git commit -m "chore: sync slopkit2 from upstream $(date -u +%Y-%m-%d)"

          echo "Committed."

      # ============================================================
      # 11. Push — rebase first to handle concurrent workflow runs
      # ============================================================
      - name: Push changes
        if: steps.changes.outputs.changed == 'true'
        run: |
          set -euo pipefail

          git pull --rebase origin "$GITHUB_REF_NAME"
          git push origin "$GITHUB_REF_NAME"

          echo "Pushed successfully."

      # ============================================================
      # 12. Nothing changed
      # ============================================================
      - name: Already up to date
        if: steps.changes.outputs.changed == 'false'
        run: echo "slopkit2 is already up to date. Nothing to push."
